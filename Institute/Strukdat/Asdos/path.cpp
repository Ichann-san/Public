#include<iostream>
#include<list>
#include<utility>
#include<vector>
using namespace std;

int main(){
    int n, k, x, i=0, res=0;
    cin >> n >> k;
    vector<pair<int, int>> gedung;
    for(int i=0; i<n; i++){
        cin >> x;
        gedung.push_back(make_pair(i, x));
    }
    res = gedung.begin.first;
    while(i!=n && i<n){

    }

return 0;
}